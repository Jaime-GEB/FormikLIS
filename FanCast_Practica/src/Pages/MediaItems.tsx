import Layout from '../layouts/Layout';
import MediaItemsComponent from '../modules/mediaItems/components/MainComponent/MediaItems';

const MediaItems: React.FC = () => {
    return (
        <Layout>
            <MediaItemsComponent />
        </Layout>
    );
};

export default MediaItems;