import Layout from '../layouts/Layout';

const MediaItemSelected: React.FC = () => {
    return (
        <Layout>
            <MediaItemSelected />
        </Layout>
    );
};

export default MediaItemSelected;